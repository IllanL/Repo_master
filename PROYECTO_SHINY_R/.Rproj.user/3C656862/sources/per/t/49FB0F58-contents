shinyUI(
  fluidPage(
    titlePanel("Hola"),
    sidebarLayout(
      sidebarPanel(
        sliderInput("nbins", label="Numero de barras", min=1, max=50, value=10, animate=T)
      ),
      mainPanel(
        plotOutput("grafica")
      )
    )
    
  )
  
)

